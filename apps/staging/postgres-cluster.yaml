apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: redmine-db
  namespace: staging
spec:
  instances: 1
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: redmine
      owner: redmine
